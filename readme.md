This visual explains what a link function is.
It lets you build different generalized linear
models for different datasets.

In all cases, you're running fitting this formula (in R formula syntax).

    y ~ x1 + x2 + x3

You can choose different link functions.
